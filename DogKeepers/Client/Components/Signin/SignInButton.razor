@inject DialogService dialogService

<button 
    type="button" 
    class="btn btn-primary"
    @onclick="@(args => dialogService.Open<SignInDialog>("",null, new DialogOptions(){ShowTitle = false}))"
>
    Entrar
</button>